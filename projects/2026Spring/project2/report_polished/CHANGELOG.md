# 排版与 typo 修复记录

- 新建 `report_polished/report.md`，保留原始 `report.md` 不变。
- 统一章节标题编号与空格（如 `## 2.实验` -> `## 2. 实验`）。
- 修复术语：`check_sum` -> `checksum`。
- 统一部分中英文间距与括号格式（如 `N（向量长度）`、`0 ns`）。
- 清理多余符号（如结尾 `****`）。
- 将同一行的多张图片拆分为多行，提升可读性。
- 新增目录（TOC），便于快速跳转。
